text/html; w3m -I %{charset} -T text/html; copiousoutput;
text/plain; less; copiousoutput;
text/calendar; icalendar %s; copiousoutput;
application/ics; icalendar %s; copiousoutput;
image/*; mkdir -p /tmp/.mutt \; cp %s /tmp/.mutt/ \; xdg-open /tmp/.mutt/"$(basename %s)" &>/dev/null &disown
application/*; mkdir -p /tmp/.mutt \; cp %s /tmp/.mutt/ \; xdg-open /tmp/.mutt/"$(basename %s)" &>/dev/null &disown
audio/*; mkdir -p /tmp/.mutt \; cp %s /tmp/.mutt/ \; xdg-open /tmp/.mutt/"$(basename %s)" &>/dev/null &disown
video/*; mkdir -p /tmp/.mutt \; cp %s /tmp/.mutt/ \; xdg-open /tmp/.mutt/"$(basename %s)" &>/dev/null &disown
